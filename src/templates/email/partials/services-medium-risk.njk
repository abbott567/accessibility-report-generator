{%- set mediumRiskServices = data.thisMonth.pdu.getServices({ risk: 'medium' }) -%}
{%- if (mediumRiskServices.length > 0) -%}
  <ul>
    {%- for service in mediumRiskServices -%}
      {%- if service.plans === 'true'-%}
        <li>{{service.name}}</li>
      {%- elif service.legacy === 'true' -%}
        <li class="disabled">{{service.name}} <span class="i">- (legacy)</span></li>
      {%- else -%}
        <li class="disabled">{{service.name}} <span class="i">- (no accessibility work scheduled)</span></li>
      {%- endif -%}
    {%- endfor -%}
  </ul>
{%- else -%}
  <p>You currently have no medium risk services.</p>
{%- endif -%}